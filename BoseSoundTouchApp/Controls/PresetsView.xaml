<UserControl
    x:Class="BoseSoundTouchApp.Controls.PresetsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BoseSoundTouchApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="320"
    d:DesignWidth="800">

    <Grid Grid.Row="1" Background="Gainsboro">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <local:PresetView x:Name="Preset1" Grid.Row="0" DataContext="{Binding Preset1}"/>
        <local:PresetView x:Name="Preset2" Grid.Row="1" DataContext="{Binding Preset2}"/>
        <local:PresetView x:Name="Preset3" Grid.Row="2" DataContext="{Binding Preset3}"/>
        <local:PresetView x:Name="Preset4" Grid.Row="3" DataContext="{Binding Preset4}"/>
        <local:PresetView x:Name="Preset5" Grid.Row="4" DataContext="{Binding Preset5}"/>
        <local:PresetView x:Name="Preset6" Grid.Row="5" DataContext="{Binding Preset6}"/>
    </Grid>
</UserControl>
